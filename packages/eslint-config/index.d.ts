import { RulesConfig } from "@eslint/core/dist/esm/types";
import { type Linter } from "eslint";

/** A shared ESLint configuration for the repository. */
export declare const config: Linter.Config<RulesConfig>[];
